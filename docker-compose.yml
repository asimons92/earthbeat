services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: earthbeat
      POSTGRES_PASSWORD: earthbeat
      POSTGRES_DB: earthbeat
    volumes:
      - earthbeat_pg:/var/lib/postgresql/data

volumes:
  earthbeat_pg:
